---
description: tab_id 的格式、驗證與使用規則（儲存、Rag/Quiz API）
globs: "**/storage.py,**/zip.py,**/grade.py,**/quiz.py,**/*rag*"
alwaysApply: false
---

# tab_id 規則

## 格式與產生

- **上傳 ZIP**：由 `utils.storage.generate_tab_id(person_id)` 產生，格式為 `{person_id}_yymmddhhmmss`（person_id 為空時用 `_`）；或由 API 呼叫端傳入。
- **repack**：可依 rag_list 衍生的檔名（如 `220222_220301`），否則用 UUID。
- **RAG 檔**：tab_id 為 `{repack_tab_id}_rag`（例如 `220222_220301_rag`）。

## 約束（必須遵守）

- **禁止路徑字元**：`tab_id` 不可包含 `/` 或 `\`。
- **不可為空**：strip 後不得為空字串。
- **驗證時機**：從 path/query/body 取得後先 `fid = (tab_id or "").strip()`，再檢查；無效時回傳 `400`（例如 "無效的 tab_id" 或 "請傳入 tab_id"）。

```python
# ✅ 正確：先 strip 再驗證
fid = (tab_id or "").strip()
if not fid or "/" in fid or "\\" in fid:
    raise HTTPException(status_code=400, detail="無效的 tab_id")
```

## 儲存路徑（utils/storage.py）

- 上傳：`storage/{person_id}/{tab_id}/upload/`（檔名可為原始檔名或 `{tab_id}.zip`）。
- repack/rag：`storage/{person_id}/{parent_tab_id}/repack/` 或 `.../rag/`，檔名為 `{tab_id}.zip`。
- 查路徑用 `get_zip_path(tab_id)` 或 `get_zip_path_by_person(person_id, tab_id)`；刪除整個 file 用 `delete_tab_folder(person_id, tab_id)`。

## 資料庫與 API

- **Rag 表**：每筆上傳對應一筆 Rag，以 `rag_tab_id` 為該 ZIP 的識別；查詢用 `.eq("rag_tab_id", fid)`，需搭配 `person_id` 或 `deleted` 等條件。
- **Quiz / Answer**：欄位 `rag_tab_id` 存來源 tab_id（upload-zip 的 source tab_id）。評分／出題時 `llm_api_key` 僅能由呼叫端傳入（從 GET /rag/rags 或 /rag/upload-zip 取得），不可從資料庫讀取；`system_prompt_instruction` 由 Rag 表該 rag_tab_id 取得。
- **回傳**：API 回傳時可帶 `"tab_id": fid` 供前端辨識。
